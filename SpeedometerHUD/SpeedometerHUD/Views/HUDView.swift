import SwiftUI

struct HUDView: View {
    var body: some View {
        VStack {
            Text("HUD View")
                .padding()
        }
        .frame(width: 260, height: 140)
    }
}

#Preview {
    HUDView()
} 